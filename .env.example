# Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# ElevenLabs voice agent
VITE_ELEVENLABS_AGENT_ID=your-agent-id

# Supabase Edge Functions only (set via Dashboard → Edge Functions → Secrets, or `supabase secrets set`)
# Do not use VITE_ — never expose this key to the browser bundle.
# ONET_API_KEY=
